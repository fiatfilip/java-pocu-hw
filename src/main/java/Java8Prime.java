import java.util.stream.IntStream;

public class Java8Prime {
    public static void main(String[] args) {
        int number = 100;
       // int sqrtN =
//        IntStream.rangeClosed(2, number)
//                .filter(n -> IntStream.rangeClosed(2, (int)Math.round(Math.floor(Math.sqrt(n)))))
//                .
    }
}
